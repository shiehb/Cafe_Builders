import { NextResponse } from "next/server";
import { adminAuth, jsonError } from "../../../../../lib/adminRoute";
import { groupsStore, validateName } from "../../../../../lib/adminStore";

export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const group = groupsStore.get((await params).id); if (!group) return jsonError("Customization group not found.", 404, "NOT_FOUND"); return NextResponse.json({ success: true, data: group }); }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const group = groupsStore.get((await params).id); if (!group) return jsonError("Customization group not found.", 404, "NOT_FOUND"); const body = await request.json(); const name = body.name === undefined ? group.name : validateName(body.name); if (!name) return jsonError("A valid customization group name is required."); Object.assign(group, { name, selectionMode: body.selectionMode === "MULTIPLE" ? "MULTIPLE" : body.selectionMode === "SINGLE" ? "SINGLE" : group.selectionMode, isRequired: body.isRequired === undefined ? group.isRequired : Boolean(body.isRequired), sortOrder: body.sortOrder === undefined ? group.sortOrder : Number(body.sortOrder), isActive: body.isActive === undefined ? group.isActive : Boolean(body.isActive), isArchived: body.isArchived === undefined ? group.isArchived : Boolean(body.isArchived) }); return NextResponse.json({ success: true, data: group, group }); }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const group = groupsStore.get((await params).id); if (!group) return jsonError("Customization group not found.", 404, "NOT_FOUND"); group.isArchived = true; group.isActive = false; return NextResponse.json({ success: true, data: group, group }); }
