import { NextResponse } from "next/server";
import { adminAuth, jsonError } from "../../../../../lib/adminRoute";
import { optionsStore, validateName } from "../../../../../lib/adminStore";

export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const option = optionsStore.get((await params).id); if (!option) return jsonError("Customization option not found.", 404, "NOT_FOUND"); return NextResponse.json({ success: true, data: option }); }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const option = optionsStore.get((await params).id); if (!option) return jsonError("Customization option not found.", 404, "NOT_FOUND"); const body = await request.json(); const name = body.name === undefined ? option.name : validateName(body.name); if (!name) return jsonError("A valid customization option name is required."); Object.assign(option, { name, priceModifier: body.priceModifier === undefined ? option.priceModifier : Math.max(0, Number(body.priceModifier) || 0), isActive: body.isActive === undefined ? option.isActive : Boolean(body.isActive), isArchived: body.isArchived === undefined ? option.isArchived : Boolean(body.isArchived) }); return NextResponse.json({ success: true, data: option, option }); }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { const denied = adminAuth(request); if (denied) return denied; const option = optionsStore.get((await params).id); if (!option) return jsonError("Customization option not found.", 404, "NOT_FOUND"); option.isArchived = true; option.isActive = false; return NextResponse.json({ success: true, data: option, option }); }
